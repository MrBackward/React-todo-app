import { css } from "@vitality-ds/system";

export default css({
  display: "flex",
  flexDirection: "column",
  gap: 4,
  alignItems: "stretch",
  width: 300,

  label: {
    fontSize: "0.95rem",
  },
});
