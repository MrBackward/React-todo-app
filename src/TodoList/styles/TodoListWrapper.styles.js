import { css } from "@vitality-ds/system";

export default css({
  display: "flex",
  flexDirection: "column",
  alignItems: "center",

  h1: {
    fontSize: "3.75rem",
    fontFamily: "Roboto, sans-serif",
  },
});
